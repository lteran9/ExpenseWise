@model IEnumerable<GroupViewModel>
@{
   ViewData["Title"] = "Expense Groups";
}

<h1 class="display-4 text-center">@ViewData["Title"]</h1>
<hr />
@if (Model?.Any() == true)
{
   @foreach (var group in Model)
   {
      <div class="card mb-4">
         <div class="card-body">
            <div class="vstack gap-3">
               <h2 class="display-5">@group.Name</h2>
               <a asp-action="Edit" asp-route-key="@group.UniqueKey" class="btn btn-secondary">Edit</a>
               <a asp-action="Invite" asp-route-key="@group.UniqueKey" class="btn btn-secondary">Invite</a>
               <span>Members: 4</span>
               <span>Spent: $0</span>
            </div>
         </div>
      </div>
   }
}
else
{
   <div class="alert alert-info">
      No groups to display.
   </div>
}

<div class="d-grid">
   <a asp-action="Create" class="btn btn-primary">+ Create Group</a>
</div>